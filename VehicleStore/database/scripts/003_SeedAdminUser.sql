-- Optional manual seed for admin user.
-- Recommended: run the API in Development mode once — it auto-creates:
--   Username: admin
--   Password: Admin@123
--
-- If you prefer SQL-only setup, generate a hash with the app's PasswordHasher
-- and replace @PasswordHash below.

USE VehicleStoreDb;
GO

-- Example placeholder — use DevelopmentDataSeeder or generate hash via API
-- INSERT INTO dbo.Users (Id, Username, Email, PasswordHash, Role, CreatedAt)
-- VALUES (NEWID(), 'admin', 'admin@vehiclestore.local', '<hash>', 'Admin', SYSUTCDATETIME());
GO
